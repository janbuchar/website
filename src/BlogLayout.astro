---
import PageLayout from "@/PageLayout.astro";
import { useTranslations } from "@i18n";

interface Props {
  title: string;
}

const t = useTranslations(Astro.currentLocale);
---

<PageLayout title={Astro.props.title} pageTitle={t("blogPageTitle")}>
  <slot slot="main" />
</PageLayout>
